
aside.sidebar
    .sidebar__header
        a.sidebar__brand(href=`${prefixAdmin}/dashboard`)  ADMIN
        
    nav.sidebar__nav
        .sidebar__menu-label Tổng quan
        a.sidebar__link(href=`${prefixAdmin}/dashboard`)
            i.fas.fa-th-large.sidebar__icon
            |  Dashboard

        .sidebar__menu-label Kinh doanh
        if(role.permission.includes("products_view"))
            a.sidebar__link(href=`${prefixAdmin}/products`)
                i.fas.fa-tshirt.sidebar__icon
                |  Quản lý sản phẩm
        if(role.permission.includes("products-category_view"))
            a.sidebar__link(href=`${prefixAdmin}/products-category`)
                i.fas.fa-list.sidebar__icon
                |  Quản lý danh mục
        a.sidebar__link(href='#')
            i.fas.fa-shopping-cart.sidebar__icon
            |  Đơn hàng
        
        .sidebar__menu-label Người dùng & Hệ thống
        a.sidebar__link(href='#')
            i.fas.fa-users.sidebar__icon
            |  Quản lý User
        if(role.permission.includes("accounts_view"))
            a.sidebar__link(href=`${prefixAdmin}/accounts`)
                i.fas.fa-user-shield.sidebar__icon
                |  Quản lý tài khoản

        .sidebar__menu-label Phân quyền
        if(role.permission.includes("roles_view"))

            a.sidebar__link(href=`${prefixAdmin}/roles`)
                i.fas.fa-users-cog.sidebar__icon
                |  Nhóm quyền
        if(role.permission.includes("roles_permissions"))
            a.sidebar__link(href=`${prefixAdmin}/roles/permissions`)
                i.fas.fa-key.sidebar__icon
                |  Phân quyền chi tiết
    .sidebar__footer
        a.sidebar__link.sidebar__link--logout(href=`${prefixAdmin}/auth/logout`)
            i.fas.fa-sign-out-alt.sidebar__icon
            |  Đăng xuất